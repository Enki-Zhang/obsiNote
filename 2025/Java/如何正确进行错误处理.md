

## 事务的传播机制

注意： 在同一个 Service 类中，方法间的直接调用（this.方法名）会导致事务失效，而通过注入的对象调用则可以生效。
#### 类比理解

想象一个场景：

- 你（目标对象）请了一个秘书（代理对象）帮你管理日程（事务）。
- 外部的人通过秘书预约你的时间（代理调用），秘书会记录并管理。
- 但如果你自己直接安排时间（this 调用），秘书不知道，也不会帮你管理。
- 如果你通过秘书安排（self 调用），秘书会介入并处理。


## 如何写try catch
